# Infra: Supabase

Project `isjkdbmfxpxuuloqosib`, Pro plan. Postgres + Auth (GoTrue) + Realtime + Storage. Single shared project across all 4 trade apps.

## Identity

- **Project ref:** `isjkdbmfxpxuuloqosib`
- **URL:** `https://isjkdbmfxpxuuloqosib.supabase.co`
- **Org:** `D4JSP org`
- **Dashboard:** <https://supabase.com/dashboard/project/isjkdbmfxpxuuloqosib>
- **SQL editor:** <https://supabase.com/dashboard/project/isjkdbmfxpxuuloqosib/sql/new>
- **Login:** "Continue with GitHub" (`@The-CowKing` / `adam87lewis@gmail.com`)

## Auth providers configured
- Email + password
- Google OAuth (Cloud Console project `d4jsp-491120`, redirect URI `https://isjkdbmfxpxuuloqosib.supabase.co/auth/v1/callback`)
- Battle.net (custom OAuth provider, configured in Auth → Providers)

## API keys

Two anon keys are valid today (must rotate the legacy one):
- **New:** `sb_publishable_uCx4acFHox_vF-rsmV-IlA_sESk1y-J` (in `next.config.js:47` and `.env.production:5`)
- **Legacy JWT:** `eyJ...` 64-yr expiry, hardcoded as fallback in `lib/supabase.js:4` and 7 other files. Revoke when fallbacks are removed.

Service role key in KVM 4 + KVM 2 prod env only. `lib/supabase-admin.js` throws if missing.

## Storage buckets

| Bucket | Public? | Purpose |
|---|---|---|
| `avatars` | yes | User profile pics (`<uid>.jpg`) |
| `assets` | yes | Trade thread attachments + admin uploads |
| `tooltip-snapshots` | yes | Wowhead PNGs (server-generated by Puppeteer) |
| `tooltip-training` | private | OCR training screenshots |

⚠ The first three buckets aren't reproducible from migrations — created via dashboard. If restoring the project from this repo alone, they won't exist.

## Realtime publication `supabase_realtime`

Currently in publication: `forum_trolls` (added by [`../../migrations/038_forum_trolls_realtime.sql`](../../migrations/038_forum_trolls_realtime.sql)).

To verify: `SELECT pubname, schemaname, tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime'`.

## Migrations

Tracked: 13 files in [`../../migrations/`](../../migrations/). **Missing 9** (015–018, 020, 023, 027–029, 031, 037) — applied directly in dashboard. **`process_trigger` PL/pgSQL function lives only in the dashboard.** Findings H-9, H-10. Fix: `pg_dump --schema-only` from production into `migrations/000_baseline.sql`.

## RLS posture

`migrations/019_users_rls_column_guard.sql` adds `WITH CHECK` on sensitive `users` columns. Other tables' RLS state is in the dashboard. See [`../data-model/rls.md`](../data-model/rls.md).

## DDL protocol

Every schema change goes through `migrations/NNN_description.sql`. Adam runs in SQL editor. Don't apply console-only changes. See [`./../conventions.md`](../conventions.md).

## Related

- [`../catalogs/system-config.md`](../catalogs/system-config.md)
- [`../data-model/migrations.md`](../data-model/migrations.md)
- [`../data-model/rls.md`](../data-model/rls.md)
- [`./credentials.md`](./credentials.md)
- [`./credential-rotation.md`](./credential-rotation.md)
